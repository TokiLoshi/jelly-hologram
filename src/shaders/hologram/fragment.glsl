void main() {

  // Adjust the normal
  
  // Add the stripes 

  // Add the Fresnel effect 
  /** 
  Formula for the fresnel effect 
  1) Get the normal vector and the view direction
  2) Calculate the dot product of the normal and the view direction
  3) Add 1 to the dot product 
  4) Reaise the result to a power
  */

  // Adjust the falloff 

  // Add the holographic effect 

  gl_FragColor = vec4(0.0, 1.0, 1.0, 1.0);
  
}